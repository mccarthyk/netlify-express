{
  "name": "test-netlify-graphql",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "netlify-lambda build lambda",
    "serve": "netlify-lambda serve lambda",
    "dev": "NODE_ENV=development netlify-lambda serve lambda"
  },
  "repository": {
    "type": "git"
  },
  "keywords": [
    "netlify",
    "graphql"
  ],
  "author": "mccarthyk",
  "dependencies": {
    "apollo-server-lambda": "^2.19.2",
    "graphql": "^15.5.0"
  },
  "devDependencies": {
    "netlify-lambda": "^2.0.3"
  }
}
